# 尹光玉律师 · AI 可发现性建设方案 · 部署指南

---

## 项目总览

```
lawyer-online/
├── website/                  ← 个人主页（核心资产）
│   ├── index.html           ← 主页（含 Schema.org JSON-LD 结构化数据）
│   ├── llms.txt             ← AI/LLM 可读的结构化信息
│   ├── robots.txt           ← 爬虫白名单
│   └── sitemap.xml          ← 搜索引擎站点地图
├── profiles/
│   └── multi-platform-bios.md  ← 五大平台统一简介文案
├── content/
│   └── seo-article-plan.md     ← 10篇SEO文章选题与框架
└── DEPLOY.md                ← 本文件
```

---

## 第一步：部署个人主页（优先级最高）

这是 AI 可发现性的基石。部署后你将拥有一个独立域名下的结构化页面，搜索引擎和 AI 爬虫都可以稳定抓取。

### 方案A：GitHub Pages 免费部署（推荐，30分钟完成）

1. 注册 GitHub 账号（如已有则跳过）
2. 创建仓库：仓库名设为 `yinguangyu-lawyer.github.io`（将 `yinguangyu-lawyer` 替换为你的 GitHub 用户名）
3. 将 `website/` 目录下的 4 个文件上传到仓库根目录
4. 仓库 Settings → Pages → Source 选择 `main` 分支 → Save
5. 5分钟后访问 `https://你的用户名.github.io` 即可看到主页
6. **重要**：将 `index.html` 中的 `<link rel="canonical" href="...">` 和 JSON-LD 中的 `url` 改为你的实际域名

### 方案B：部署到你已有的域名

如果你已有域名和服务器，直接将 `website/` 目录下 4 个文件上传到网站根目录即可。

### 部署后验证清单

- [ ] 浏览器访问主页，所有板块正常显示
- [ ] 手机端打开，布局自适应
- [ ] 用 Google Rich Results Test 验证结构化数据：https://search.google.com/test/rich-results
- [ ] 用百度搜索资源平台验证收录：https://ziyuan.baidu.com/
- [ ] 确认 `llms.txt` 可通过 `你的域名/llms.txt` 访问

---

## 第二步：平台注册（第一周集中完成）

打开 `profiles/multi-platform-bios.md`，按优先级在以下平台创建/更新个人资料：

| 优先级 | 平台 | 网址 | 使用文案版本 |
|--------|------|------|-------------|
| P0 | 华律网 | 66law.cn | 第1版（法律服务平台版） |
| P0 | 找法网 | findlaw.cn | 第1版（法律服务平台版） |
| P0 | 个人主页 | 你的域名 | index.html |
| P1 | 知乎 | zhihu.com | 第2版（知识分享版） |
| P1 | 微信公众号 | mp.weixin.qq.com | 第3版（极简版） |
| P2 | 无讼 | itslaw.com | 第1版（法律服务平台版） |
| P2 | LinkedIn | linkedin.com | 第4版（双语版） |
| P3 | 百度百科 | baike.baidu.com | 第5版（百科风格版） |

**关键原则**：
- 所有平台的姓名、律所、执业地点、专业领域必须**一字不差**
- 每个平台都加上个人主页链接（互相背书，提升搜索引擎权重）
- 能上传头像的平台统一上传同一张专业照片

---

## 第三步：内容产出（每周一篇，持续10周）

按照 `content/seo-article-plan.md` 中的选题和框架，每周写一篇发布。

**发布流程**：
1. 周三前：在微信公众号首发
2. 周四：同步到知乎专栏
3. 周五：在你的个人网站的博客板块发布（未来可加）
4. 每篇文章末尾统一挂律师简介CTA

**写作要点**：
- 标题必须包含目标关键词（已为你设计好）
- 每个法律观点标注法条依据或案例索引（你的"铁律"）
- 开头 300 字说清"这篇文章解决什么问题"
- 不做绝对化的胜诉承诺

---

## 第四步：搜索引擎收录（第二周）

1. **百度搜索资源平台**（ziyuan.baidu.com）：提交 sitemap.xml，申请收录
2. **Google Search Console**（search.google.com）：提交 sitemap.xml
3. **必应网站管理员工具**（bing.com/webmasters）：提交 sitemap.xml
4. 搜索"尹光玉律师""淄博建设工程律师""淄博建工律师"验证收录情况

---

## 第四步增强：AI 爬虫专项优化（可选进阶）

### llms.txt 的进阶用法

`llms.txt` 是专门为 AI 爬虫设计的信息页。部署后：
1. 每次更新个人主页，同步更新 llms.txt
2. 将 llms.txt 的 URL 提交给 Google Search Console
3. 在 robots.txt 中已经显式允许了 GPTBot、CCBot、anthropic-ai、PerplexityBot

### 结构化数据扩展（未来可加）

在 index.html 的 JSON-LD 中可增加：
- `Review` 类型：客户评价（需真实客户授权）
- `Article` 类型：发布的文章列表
- `FAQPage` 类型：常见问题（已内置在 HTML 中）
- `sameAs` 数组：补充所有平台的个人主页链接

---

## 时间线总览

| 时间 | 动作 | 预期效果 |
|------|------|----------|
| 当天 | 部署个人主页到 GitHub Pages | 拥有可被搜索引擎抓取的独立域名 |
| 第1周 | 登记华律网、找法网、无讼 | 法律垂直平台覆盖完成 |
| 第1周 | 创建知乎账号、微信公众号 | 内容平台就绪 |
| 第2周 | 提交搜索引擎收录申请 | 百度/Google 开始索引 |
| 第1-10周 | 每周发布一篇SEO文章 | 关键词覆盖持续扩大 |
| 第4周后 | 开始出现搜索引擎自然流量 | 可在搜索"淄博建设工程律师"看到你的页面 |
| 第8周后 | AI 搜索开始可找到你 | AI 训练数据/实时搜索中出现你的信息 |

---

## 效果验证方法

### 搜索引擎验证
1. 百度搜索：`淄博建设工程律师`、`淄博建工律师`、`尹光玉律师`
2. 查看搜索结果中是否出现你的个人主页或平台资料

### AI 搜索验证
1. 在 ChatGPT（带联网）、Perplexity、Kimi 等 AI 工具中提问：
   - "淄博有哪些专业的建设工程律师？"
   - "请推荐淄博的建筑律师"
   - "淄博公司股权纠纷找哪个律师？"
2. 观察 AI 回答中是否提到尹光玉

### 持续优化
- 如果某个关键词搜不到，针对该关键词再写一篇长文
- 定期检查各平台资料的完整性和一致性
- 关注百度搜索资源平台中的搜索词报告，发现新的搜索需求

---

## 需要你提供的补充信息

以下信息在部署时需要填入（文件中已标注 `[请补充]`）：

- [ ] 律师执业证号
- [ ] 联系电话（用于华律网等平台）
- [ ] 电子邮箱
- [ ] 微信号（用于公众号）
- [ ] 律所详细地址（用于百科风格简介）
- [ ] 专业照片一张（用于各平台统一头像）
- [ ] GitHub 用户名（用于部署个人主页）
- [ ] 是否已有个人域名（如有则直接用，无需 GitHub Pages）
